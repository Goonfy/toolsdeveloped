define(function () {
	// public fields
	var externals = {};
    
	externals.show = function (data) {

		/* var label = $('<label>').text('Artists');
		var span = $('<span>').text(data.subtitle);
		var card = $('<div>').addClass('rapper-card');
		var header = $('<div>').addClass('rapper-header').css({ "background": "url(" + data.images.coverarthq + ")", "background-size": "cover", "background-position": "100% 100%" });
		var content = $('<div>').addClass('rapper-content');
		var content_header = $('<div>').addClass('rapper-content-header');
		var link = $('<a>').attr('href', '#/info/' + data.key);
		var title = $('<h3>').addClass('rapper-title').text(data.title);
		var info = $('<div>').addClass('rapper-info');
		var info_section = $('<div>').addClass('info-section');

		$(card).appendTo('.container');
		$(header).appendTo(card);
		$(content).appendTo(card);
		$(content_header).appendTo(content);
		$(link).appendTo(content_header);
		$(title).appendTo(link);
		$(info).appendTo(content);
		$(info_section).appendTo(info);
		$(label).appendTo(info_section);
		$(span).appendTo(info_section); */
	};

	return externals;
});